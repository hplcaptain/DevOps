version: '3'

services:

  frontend:
    build: ./frontend
    container_name: frontend-app

    ports:
      - "3000:3000"

    depends_on:
      - backend

  backend:
    build: ./backend
    container_name: backend-app

    ports:
      - "5000:5000"